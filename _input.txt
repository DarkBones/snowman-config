● You're right, and I should have questioned this earlier. If Tahoe has been out for 8 months and others
  are using nixpkgs on it, the problem is likely something else.

  Let me think about this differently: why is LLVM being built from source instead of fetched from the
  binary cache?

  Can you run these on your mbp to check your nix configuration:

  # Check what binary caches are configured
  nix show-config | grep -E "substituters|trusted-public-keys"

  # Check if the specific LLVM derivation exists in the cache
  nix path-info --store https://cache.nixos.org
  /nix/store/pavgzlpn7hrv4wsbl2sgycjkny1zpjfq-llvm-16.0.6.drv 2>&1

  If the binary cache is working and has this derivation, it should be fetched, not built. The fact that
  429 packages are being fetched but LLVM isn't suggests either:
  1. A cache configuration issue on your mbp
  2. Or that specific derivation hash isn't in the cache for aarch64-darwin

  Also - what does nix --version show on the mbp?

